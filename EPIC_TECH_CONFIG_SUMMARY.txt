Epic - Advanced Technical Configuration Implementation Summary:
1.  **Data Model**: Updated `Tenant` interface in `lib/data.ts` to include `ssoConfig`, `integrations`, and expanded `policies`.
2.  **UI Implementation**: Created `app/dashboard/admin/settings/page.tsx` with 3 tabs:
    *   **Authentication**: Configure SSO (OAuth2, SAML, etc.) or Native login. Validates required fields.
    *   **Integrations**: List, Add, Toggle, Delete external APIs (REST/GraphQL).
    *   **Security Policies**: Configure Session Duration, Max Failed Logins, 2FA enforcement, and IP Whitelist.
3.  **Navigation**: Added 'Configuración Técnica' link to Sidebar for Tenant Admins (Level 1 Users).
4.  **Logic**: Validated save logic updates the mock DB via `updateTenant`.

Ready for testing as Tenant Admin.
